<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>CRUD Nodejs y MongoDb</title>
    <!-- BOOTSTRAP CDN -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.0/css/bootstrap.min.css" integrity="sha384-9gVQ4dYFwwWSjIDZnLEWnxCjeSWFphJiwGPXr1jddIhOegiu1FwO5qRGvFXOdJZ4" crossorigin="anonymous">
  </head>
  <body>
    <nav class="navbar navbar-dark bg-dark mb-4">
      <a class="navbar-brand" href="#">Dashboard</a>
      <p style="color:white"> Welcome <%= user.name %></p>
      <a style="color:white" href="/users/logout">Logout</a>
    </nav>

<div class="container">
  <div class="row">
    <!--FORM-->
    <div class="col-md-5 offset-md-3">
      <div class="card">
        <div class="card-body">
          <form action="/dashboard/edit/<%= task._id %>" method="post">
            <div class="form-group">
              <input class="form-control" type="text" name="nombre" placeholder="Nombre" value="<%= task.nombre %>">
            </div>
            <div class="form-group">
              <input class="form-control" type="number" name="ciclo"  placeholder="Agregar ciclo" value="<%= task.ciclo%>">
            </div>
            <div class="form-group">
              <input class="form-control" type="number" name="nota"  placeholder="Agregar nota" value="<%= task.nota %>">
            </div>
            <div class="form-group">
              <textarea class="form-control" name="observacion"  placeholder="Agregar observación"
                ><%= task.observacion %></textarea>
            </div>
            <div class="form-group">
              <textarea class="form-control" name="descripcion" cols="80" placeholder="Agregar descripción"
                ><%= task.descripcion %></textarea>
            </div>
            <button class="btn btn-primary" type="submit">
              Update 
            </button>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>
</body>
</html>